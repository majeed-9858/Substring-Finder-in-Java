# Substring-Finder-in-Java
This Java program demonstrates how to extract a substring from a string using the substring() method. It includes examples for:  Extracting a substring starting from a specific index (substring(startIndex))  Extracting a substring between two indexes (substring(startIndex, endIndex))
